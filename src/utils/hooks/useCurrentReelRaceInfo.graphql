query CurrentReelRaceInfoQuery {
  reelRaces(limit: 2) {
    id
    game {
      #   id: slug
      slug
      name
      logo
      backgroundImage
    }
    startTime
    endTime
    optedIn
    promoted
    spinLimit
    cometdChannels
    leaderboard {
      #   id: playerId
      playerId
      position
      points
      remainingSpins
    }
  }
}
