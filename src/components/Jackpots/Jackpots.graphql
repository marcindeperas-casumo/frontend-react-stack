#import "../GameRow/GameRow.graphql"

query JackpotsQuery($number: Int!) {
  gamesList(listId: "casumoJackpotGames") {
    name
    games(number: $number) {
      ...Jackpots_Game
    }
  }
}

fragment Jackpots_Game on Game {
  ...GameRow_Game
}
