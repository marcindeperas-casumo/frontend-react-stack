query GetGameSets {
  gameSetsList {
    id
    key
    title
    icon
    defaultSort
    supportedSorts
    baseQuery
    gameDisplayMode
    additionalFilterGroups {
      key
      type
      title
      description
      values {
        key
        query
        title
      }
    }
  }
}
