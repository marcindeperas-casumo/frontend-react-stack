#import "../GameTile/GameTile.graphql"

query GameListPageQuery($query: String!, $offset: Int!, $limit: Int!) {
  getGamesForSet(query: $query, offset: $offset, limit: $limit) {
    gamesCount
    offset
    games {
      ...GameTile_Game
    }
  }
}
