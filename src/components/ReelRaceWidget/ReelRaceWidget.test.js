// @flow
// import React from "react";
// import { shallow } from "enzyme";
// import { ReelRaceWidget } from "./ReelRaceWidget";

describe("ReelRaceWidget", () => {
  describe("Scheduled", () => {
    // const now = Date.now();

    test("should", () => {
      // expect(rendered.contains(props.t.starting_in)).toBe(true);
    });
  });

  describe("Ongoing", () => {
    // const now = Date.now();

    test("should ", () => {
      // expect(rendered.contains(props.t.starting_in)).toBe(true);
    });
  });
});
