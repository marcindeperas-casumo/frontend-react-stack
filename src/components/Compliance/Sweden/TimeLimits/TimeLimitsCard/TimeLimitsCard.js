// @flow
import * as React from "react";

type Props = {
  onClick: () => void,
};

export function TimeLimitsCard({ onClick }: Props) {
  return (
    <div onClick={onClick} className="u-padding--lg">
      Not yet implemented Time Limits Card
    </div>
  );
}
