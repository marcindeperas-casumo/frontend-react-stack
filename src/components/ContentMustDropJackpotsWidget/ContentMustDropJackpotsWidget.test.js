import React from "react";
import { shallow } from "enzyme";
import ContentMustDropJackpotsWidget from "Components/ContentMustDropJackpotsWidget";

describe("ContentMustDropJackpotsWidget", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
