fragment GameTileInMaintenance_Game on Game {
  backgroundImage
  logo
  name
}

query GameTileInMaintenanceCmsQuery {
  temporaryUnavailableText: getText(
    id: "root:mobile.game-details:fields.temporarily_unavailable"
  )
}
