fragment GameRow_Game on Game {
  id
  backgroundImage
  logo
  name
  slug
  lobby {
    bets {
      min
      max
      symbol
    }
  }
  jackpot {
    id
    value {
      amount
      currency
    }
  }
}
