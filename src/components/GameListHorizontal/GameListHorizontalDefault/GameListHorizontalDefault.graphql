#import "../../GameTile/GameTile.graphql"

query GameListQuery($id: String!, $number: Int!) {
  gamesList(listId: $id) {
    id
    name
    games(number: $number) {
      ...GameTile_Game
    }
  }
}
