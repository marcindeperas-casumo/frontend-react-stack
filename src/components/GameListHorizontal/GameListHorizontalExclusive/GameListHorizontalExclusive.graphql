#import "../../GameTile/GameTile.graphql"

query GameListExclusiveQuery($id: String!, $number: Int!) {
  gamesList(listId: $id) {
    id
    name
    games(number: $number) {
      ...GameTile_Game
    }
  }
}
