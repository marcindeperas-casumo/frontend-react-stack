import React from "react";
import { shallow } from "enzyme";
import MustDropListItem from "./MustDropListItem";

describe("MustDropListItem", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
