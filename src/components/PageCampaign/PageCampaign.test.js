import React from "react";
import { shallow } from "enzyme";
import PageCampaign from "Components/PageCampaign";

describe("PageCampaign", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
