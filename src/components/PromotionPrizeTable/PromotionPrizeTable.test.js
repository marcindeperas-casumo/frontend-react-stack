import React from "react";
import { shallow } from "enzyme";
import PromotionPrizeTable from "Components/PromotionPrizeTable";

describe("PromotionPrizeTable", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
