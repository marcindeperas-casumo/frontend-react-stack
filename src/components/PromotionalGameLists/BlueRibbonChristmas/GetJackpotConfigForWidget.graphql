query GetJackpotConfigForWidget($slug: String!) {
  blueribbonJackpot(slug: $slug) {
    externalId
    title
    image
    slug
    pots {
      externalId
      potKey
      name
      shortName
      mainWinRatio
      sharedPot { 
        name
        shortName
        icon
      }
    }
  }
}	