import React from "react";

export const JackpotRules = ({ text }) => {
  return (
    //todo - replace {{ tcLink }} with clickable link to open t&c modal
    <div className="u-padding t-border t-border-r t-border-grey-5">{text}</div>
  );
};
