import React from "react";
import { shallow } from "enzyme";
import MustDropJackpotDetailList from "Components/MustDropJackpotDetailList";

describe("MustDropJackpotDetailList", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
