import React from "react";
import { shallow } from "enzyme";
import PromotionGalleryCard from "Components/PromotionGalleryCard";

describe("PromotionCardGallery", () => {
  test("should do something", () => {
    expect(1).toBe(2);
  });
});
