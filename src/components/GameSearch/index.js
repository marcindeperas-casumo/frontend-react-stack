export { GameSearchContainer as GameSearch } from "./GameSearchContainer";

// HEADS UP!
// Only needed until we replace all the default exports
// and until we update the Lazy components to cater for
// named exports as well.
export { GameSearchContainer as default } from "./GameSearchContainer";
