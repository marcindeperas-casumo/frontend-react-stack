#import "../ReelRaceScheduleCard/ReelRaceScheduleCard.graphql"

query ReelRacesPageQuery($limit: Int!, $previous: Boolean) {
    reelRaces(limit: $limit, previous: $previous) {
        id
        ...ReelRaceScheduleCard_ReelRace
    }
}