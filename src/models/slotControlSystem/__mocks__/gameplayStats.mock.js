//@flow
import { type GameplayStatsType } from "Models/slotControlSystem";

export default ({
  consumedBalance: 8,
  remainingBalance: 32,
  limit: 40,
  totalBets: 5,
  totalWins: 11,
  lastUpdateTime: null,
} : GameplayStatsType);