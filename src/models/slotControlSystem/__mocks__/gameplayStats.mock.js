//@flow
import { type GameplayStatsType } from "Models/slotControlSystem";

export default ({
  currency: "EUR",
  consumedBalance: 8,
  remainingBalance: 32,
  initialLimit: 40,
  totalBets: 5,
  totalWins: 11,
  lastUpdateTime: null,
} : GameplayStatsType);